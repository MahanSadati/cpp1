#include <iostream>
#include <conio.h>
using namespace std;
main(){
    int a , b;
    cout << "enter a:"; // input a variable
    cin >> a;
    cout << "enter b:"; // input b variable
    cin >> b;
    cout << "a + b = " << a + b << endl;
    cout << "a * 2 = " << a * 2 ;       // outputs
    getch();
}